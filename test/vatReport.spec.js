import expect from 'expect'
import counter from '../src/reducers'

describe('reducers', () => {
    describe('counter', () => {

        it('should ignore unknown actions', () => {
            expect(1).toBe(1)
        })
    })
})