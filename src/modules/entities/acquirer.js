const initialState = {
  data: [
    {code: 'ALPHA', name: 'Alpha bank'},
    {code: 'PAYCARD', name: 'Paycard'}
  ]
}

export default function acquirerDropdownReducer (state = initialState, action) {
  return state
}
