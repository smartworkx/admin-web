import expect from "expect";

describe('reducers', () => {
  describe('counter', () => {

    it('should ignore unknown actions', () => {
      expect(1).toBe(1)
    });

    it('should ignore unknown actions 2', () => {
      expect(1).toBe(1)
    })
  })
});


